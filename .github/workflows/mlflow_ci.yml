name: Retrain and Package Model CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  retrain-model:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mlflow scikit-learn pandas numpy

      - name: Install Conda (required for MLflow project environment)
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          python-version: 3.11

      - name: Run MLflow Project Retraining
        working-directory: MLProject
        run: |
          mlflow run . --no-conda -P n_estimators=120 -P max_depth=6

      # OPTIONAL (Docker build & push)
      # aktifkan kalau sudah setup secrets Docker Hub
      #
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Build and Push Docker Image
      #   run: |
      #     mlflow models build-docker \
      #       -m "runs:/${{ steps.get-run-id.outputs.run_id }}/tuned_random_forest_model" \
      #       -n "${{ secrets.DOCKER_USERNAME }}/loan-approval-retraining:latest"
      #     docker push ${{ secrets.DOCKER_USERNAME }}/loan-approval-retraining:latest